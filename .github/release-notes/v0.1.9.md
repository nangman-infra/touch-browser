- Added a global `touch-browser --version` / `-V` entrypoint so the installed CLI can report the managed runtime version directly.
- Aligned the Rust workspace version, MCP bridge implementation version, MCP registry metadata, and npm package version on `0.1.9`.
- Kept release-facing version reporting consistent across the standalone CLI, the local MCP bridge, and the npm-distributed MCP package.
