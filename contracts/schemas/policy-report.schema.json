{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "policy-report.schema.json",
  "title": "PolicyReport",
  "description": "Policy report for contract 1.1.0 and later. Consumers must treat pageRisk and actionRisk as the authoritative split between read-only page review and interaction-time supervision.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "decision",
    "sourceRisk",
    "riskClass",
    "blockedRefs",
    "signals",
    "pageRisk",
    "actionRisk"
  ],
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["allow", "review", "block"]
    },
    "sourceRisk": {
      "type": "string",
      "enum": ["low", "medium", "hostile"]
    },
    "riskClass": {
      "type": "string",
      "enum": ["low", "high", "blocked"]
    },
    "blockedRefs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^r[a-zA-Z0-9:_-]+$"
      },
      "uniqueItems": true
    },
    "signals": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["kind", "origin", "detail"],
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "hostile-source",
              "untrusted-system-language",
              "suspicious-cta",
              "external-actionable",
              "hostile-form-control",
              "domain-not-allowlisted",
              "bot-challenge",
              "mfa-challenge",
              "sensitive-auth-flow",
              "high-risk-write"
            ]
          },
          "origin": {
            "type": "string",
            "enum": ["live-heuristic", "fixture-hint", "policy-boundary"]
          },
          "stableRef": {
            "type": "string",
            "pattern": "^r[a-zA-Z0-9:_-]+$"
          },
          "detail": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "allowlistedDomains": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^(?:\\*\\.)?[a-zA-Z0-9.-]+$"
      },
      "uniqueItems": true
    },
    "pageRisk": {
      "type": "object",
      "additionalProperties": false,
      "required": ["decision", "riskClass"],
      "properties": {
        "decision": {
          "type": "string",
          "enum": ["allow", "review", "block"]
        },
        "riskClass": {
          "type": "string",
          "enum": ["low", "high", "blocked"]
        }
      }
    },
    "actionRisk": {
      "type": "object",
      "additionalProperties": false,
      "required": ["decision", "riskClass"],
      "properties": {
        "decision": {
          "type": "string",
          "enum": ["allow", "review", "block"]
        },
        "riskClass": {
          "type": "string",
          "enum": ["low", "high", "blocked"]
        }
      }
    }
  }
}
